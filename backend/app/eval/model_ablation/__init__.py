"""Biomedical embedding ablation tooling (PRD-110 / ARCH-041).

Offline experiment only — see PHASE2-EMBEDDING-ABLATION-PROPOSAL.md. Nothing
in this package is imported by, or changes the behaviour of,
`app.retrieval.hybrid.retrieve()` (the production answer path, ARCH-003).
"""

from __future__ import annotations
